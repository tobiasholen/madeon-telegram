# Tasks

| Task | Einfach | Sophisiticated |
| --- | ---- | --- |
| Finden von starken usern auf telegram durch gruppen | manuel web durchsuchen | |
| Account Warmup | abwarten dass der account alter wird (0d) | Mit dem account etwas manuel schreiben / oder Cupit verwenden (0d) | API verwenden um sporadisch message zu verwenden (2d)|
| Scrap users from group chats and put them into a database and filter tabase for duplicates | implement it using telegram API (1d) |
| send first message using telegram api to one of the scraped users | using a message table and choosing random message (1d) |
| using cupid on the desktop to chat | send notification to Desktop if we have a reploy to the first message (2d) |
using cupid on the server to make the whole chat | | deploying the chatbot on the cliud; using different IP addresses for different bot accounts; checking if we still have conversations left with cupit and messaging operator if we need more |
| **Total** | **4d** | |